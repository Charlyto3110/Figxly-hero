import { byRubro } from './catalog.js';

export function renderCostGeneratorUI(rubro, draft = {}) {
  const activities = byRubro(rubro);
  return `<div class="space-y-4"><label class="block text-sm">Rubro<select name="rubro" class="mt-1 w-full rounded-xl border border-slate-200 p-2 bg-white"><option value="${rubro}">${rubro}</option></select></label><div class="space-y-2">${activities.map((a) => `<label class="flex items-center gap-2 rounded-xl border border-slate-200 p-2"><input type="checkbox" name="activity" value="${a.id}" ${draft.activities?.some((d) => d.activityId === a.id) ? 'checked' : ''} /><span class="flex-1 text-sm">${a.nombre} (${a.unidad})</span><input type="number" min="0" step="1" name="qty-${a.id}" value="${draft.activities?.find((d) => d.activityId === a.id)?.qty || 1}" class="w-20 rounded-lg border border-slate-200 p-1" /></label>`).join('')}</div><div class="rounded-xl border border-slate-200 p-3"><p class="font-semibold">Extra</p><input name="extraDescription" placeholder="Descripción" class="mt-2 w-full rounded-xl border border-slate-200 p-2" /><div class="mt-2 grid gap-2 sm:grid-cols-2"><input name="extraQty" type="number" min="1" value="1" class="rounded-xl border border-slate-200 p-2" /><input name="extraPrice" type="number" min="0" placeholder="Precio unitario" class="rounded-xl border border-slate-200 p-2" /></div><input name="extraReason" placeholder="Motivo" class="mt-2 w-full rounded-xl border border-slate-200 p-2" /><input name="extraEvidence" type="file" accept="image/*" class="mt-2 w-full rounded-xl border border-slate-200 p-2" /></div></div>`;
}
